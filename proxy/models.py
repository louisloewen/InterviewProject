"""Canonical data models (STUB — Issue #1).

Will hold the canonical ``Employee`` Pydantic model and the paginated response
schema returned by ``GET /employees``. Empty for Issue #0 scaffolding.
"""
